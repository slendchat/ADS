ПРИМЕР 1
1.(3) ошибка в компиляции произошла изза того что функция hello не была продекларирована. Но даже если бы была
    то линкер не знал бы с каким адрессом связать имя данной функции

ПРИМЕР 2
2.(4) происходит ошибка error: duplicate symbol изза того что в symbol table попадают имена функций после компиляции
    при стадии линкинга и два объектных файла имеют свои собственные символы для функции
    что приводит к тому что линкер отбрасывает дубликат

3.(6) Ошибка функция hello неопределена, появляется ошибка на стадии линковки так как символ есть но никуда
    не ссылается

4.(8) Снова на стадии линковки появляется ошибка так как функция hello определена только в
    f.cpp и изза модификатора static недоступна извне. Поэтому снова функция объявлена но не определенна.

5.(10) Работает теперь благодаря тому что функция определена и объявлена уже в файле main.cpp 
    благодаря чему при линковки функция main знает куда обращатся при вызове hello

6.(11) Копий тело функции hello будет два в финальном екзешнике так как будет существовать обе функции 
    для двух compilation unit